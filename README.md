
- Designed and executed SQL queries to analyze structured business data  
- Used **SELECT, WHERE, GROUP BY, HAVING, ORDER BY** clauses for data analysis  
- Implemented **INNER JOIN, LEFT JOIN** to handle one-to-many relationships between tables  
- Created calculated fields using **CASE WHEN** logic  
- Performed aggregations using **SUM, COUNT, AVG, MIN, MAX**  
- Applied data filtering to identify key trends and performance metrics  
- Ensured data consistency and accuracy during analysis  
- Optimized queries for better readability and performance  
- SQL output used as a base for further analysis and Power BI dashboards  

These queries support data-driven insights and can be reused for reporting and visualization purposes.
